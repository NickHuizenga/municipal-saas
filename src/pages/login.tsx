import React from 'react';

const Page: React.FC = () => {
  return (
    <div style={{minHeight:'100vh',display:'grid',placeItems:'center',color:'#e5e5e5',background:'#0a0a0a'}}>
      <div>Login (pages router stub) — if you see this, routing works.</div>
    </div>
  );
};

export default Page;
